import 'package:flutter/material.dart';

class RequestManagePage extends StatelessWidget {
  const RequestManagePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
